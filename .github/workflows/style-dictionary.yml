name: Build & Release Design Tokens

on:
  push:
    branches:
      - main
    paths:
      - "token.json"

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build tokens
        run: npm run build:tokens

      - name: Commit build results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if [[ -n "$(git status --porcelain)" ]]; then
            git add build
            git commit -m "chore(tokens): build design tokens"
          fi

      - name: Bump version & tag
        run: |
          npm version patch -m "chore(release): %s"
          git push --follow-tags https://${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ### 🎨 Design Tokens Updated
            - token.json 변경 반영
            - iOS / Android / Web 토큰 자동 생성
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
